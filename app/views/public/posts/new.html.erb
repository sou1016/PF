<h1>投稿フォーム</h1>
<%= form_with model: [:public, @post] do |f| %>
  <h4>画像</h4>
    <%= f.file_field :image %>
  <h4>ジャンル</h4>
    <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <h4>概要</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
  <% end %>